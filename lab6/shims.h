/* Neil Kirby */
/* lab 6 headers */

/* the shims - call these */
int ex_shim( char *inptr, char *buffer);
int m_shim(char *ptr );
char * s_shim(char **strings, int count);

/* the functions that the shims call - do not call these */
//int expand( char *inptr, char *buffer);
//int middle(char *ptr );
//char *search(char **strings, int count);
